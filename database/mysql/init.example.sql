-- Este arquivo é apenas um exemplo e NÃO é carregado pelo container MySQL
-- Mantenha aqui comandos de referência, caso precise recriar manualmente algo.
-- O bootstrap real do schema deve ocorrer via migrations Laravel.

-- Exemplo de criação de usuário extra (não necessário se usar variáveis de ambiente):
-- CREATE USER 'readonly'@'%' IDENTIFIED BY 'readonly_pass';
-- GRANT SELECT ON define_pilates.* TO 'readonly'@'%';

-- Exemplo de ajuste de collation:
-- ALTER DATABASE `define_pilates` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Não adicionar CREATE TABLE aqui. Use migrations.
